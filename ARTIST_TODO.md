# 仍需画师返工的资产问题

以下是 `tools/fix_assets.py` **修不了**的部分 —— 全都需要重新绘制，脚本改不出来。已按严重度排序，每条都写了具体怎么改和验收标准。

先说明两条**我之前判断错了、现在撤销**的：

- ~~R4 · P-11「金属内拐角有黑洞」~~ —— **误报**。放大看那是有意设计的内拐角缺口，与包边逻辑一致，做得是对的。
- ~~「charge 帧逐帧翻转 90°」~~ —— **误报**。用连通域重新量过，charge 六帧的主体长宽比稳定在 0.70–0.72，朝向是一致的。之前的长宽比异常是帧内游离碎屑撑大了包围盒造成的假象。真正的问题是下面的 A-01。

---

## A-01　主角 charge / thrust / glide 三个 Tag 的体积在 Tag 内部剧烈波动

**这是唯一会被玩家直接看出来的问题**，其余都是接入期的技术债。

用连通域取最大主体（排除碎屑）后的实测像素数：

| Tag | 逐帧主体像素 | 波动 |
|---|---|---|
| `idle` | 973 / 1023 / 1032 / 1036 / 882 / 1004 / 1010 / 1029 | 15% ✅ |
| `pulse` | 883 / 923 / 908 / 835 / 918 / 942 / 877 / 902 | 11% ✅ |
| `recover` | 800 / 717 / 982 / 950 | 27% ⚠️ |
| `charge` | **437 / 229 / 430 / 231 / 76 / 100** | **83%** ❌ |
| `glide` | **919 / 264 / 239 / 365 / 473 / 493 / 453 / 855** | **74%** ❌ |
| `thrust` | 550 / 485 / 485 / 604 / 367 / 501 / 899 / 899 | 59% ❌ |

`charge` 的第 5、6 帧只剩 76 和 100 像素 —— 是 `idle` 的 **8%**，本体基本消失了。`glide` 在一个循环里从 919 掉到 239 再回到 855。

**要求：**
- 以 `idle` 的主体像素数（约 1000）为基准重画这三个 Tag
- 压扁、拉伸、运动模糊都可以改变轮廓，但**主体像素数波动控制在 ±25% 以内**
- 唯一例外：`thrust` 的高速帧因运动模糊变细可放宽到 -40%，但首尾帧必须与相邻 Tag 衔接得上

**验收：** 把每个 Tag 单独循环播放，主角不应有可见的"缩水—膨胀"呼吸感。

**目前的绕过方式：** 游戏只播 `idle` / `pulse` 的原始帧，charge/thrust/glide 改用程序化压扁拉伸。调参台第一个开关可以切回原始帧，能直接看到问题。

---

## A-02　主角图集缺 pivot

`jellyfish_character_sheet.json` 的 `meta` 里没有 slices，未定义锚点。引擎只能按 48×48 帧中心对齐，叠加 A-01 的体积波动会明显抖。

**要求：** 在 Aseprite 里加一个名为 `pivot` 的 Slice（建议锚在伞盖顶端中点），所有 Tag 共用同一锚点，随 JSON 一起导出。

---

## A-03　警觉态机械眼与巡逻态不是同一台机器

实测包围盒：

- 巡逻本体 `(9,8)–(39,39)`，约 30×31 px，645 不透明像素
- 警觉本体 `(14,4)–(33,43)`，约 19×39 px，280 不透明像素

警觉态只有巡逻态 **43%** 的面积，且更窄更高、锚点位置不同。状态切换瞬间机器人会缩水并跳位。

**要求：** 警觉态以巡逻态的球体直径与圆心为基准重画。机械瓣裂开时轮廓可以变，但球体主体的直径和中心必须不变。

**目前的绕过方式：** 游戏给警觉态贴了 1.35× 的缩放补偿。这只是权宜之计，裂开的机械瓣被一起放大了，细节比例不对。

---

## A-04　闸门阀轮凭空消失

`pressure_gate` 第 3 帧到第 4 帧之间，中央三叶阀整体消失。阀轮位于门缝中央，门板向两侧滑开时它应当**一分为二跟随门板移动**，而不是蒸发。

**要求：** 补齐阀轮在第 4、5 帧的左右两半，跟随门板滑出画面。

> 阀轮的颜色已由脚本从警示红 `#FF3344` 改成锈橙 `#D8792D`（警示红要留给危险源），重画时请沿用锈橙。

---

## A-05　闸门门框仍烘焙在贴图内

门框画死在 64×64 内，无法与关卡墙体自由拼接。当前关卡是围着这张图的尺寸设计的（挖了正好 2×2 格的洞），但后面的关卡不一定合适。

**要求：** 门框拆成独立图块，或额外提供一版无框的纯门板。

---

## A-06　视锥被帧边界硬切

视锥包围盒每帧都正好停在 x=48（帧右边界），有效长度仅 21 px，末端是一条垂直硬切线。引擎需要按巡逻范围动态调整视锥长度，当前资产既没有自然收尾，也没有可平铺延展的中段。

**要求：** 二选一 —— (a) 视锥单独出图，画布加宽到能容纳最长视锥；(b) 拆成「根部 + 可平铺中段 + 末端」三件。

**目前的绕过方式：** 游戏里视锥是程序画的（单色 50% 棋盘格抖动三角形），长度可调。如果视锥造型要更复杂，还是得走上面的方案。

---

## A-07　seaweed / mycelium / server_cabinet 的帧语义不明

`ruin_ecosystem_tileset.json` 里：`seaweed` 占 105–110（6 帧），`mycelium` 占 114–119（6 帧）。

**这 6 帧是 6 个静态变体，还是 3 个变体 × 2 帧动画？** JSON 没有区分，程序无法接入。另外这三个 `.aseprite` 源文件都没有对应的 PNG + JSON 导出。

**要求：** 明确说明帧结构；若为动画，拆成更细的 Tag（如 `seaweed_a` / `seaweed_b` / `seaweed_c`）或单独导出。

---

## A-08　菌丝的"发光"感偏弱

呼吸两帧之间只有 4 px 高光差异（`#DFFFF7`）。设定是「荧光发光菌丝」，当前更接近普通苔藓。

**要求：** 加强明暗差，或增加 1–2 px 的外发光轮廓。在全黑的 Zone 1/2 里，发光植被是重要的空间提示。

---

## A-09　autotile 覆盖测试不完整

包内 `tests/autotile_coverage.txt` 自述：47 个 mask 中测试仅覆盖 29 个。

**不过这条现在优先级降低了** —— 引擎侧已经从图块自身的包边亮度反推出完整的 47 掩码表，并在真实关卡的 1388 个实心格上跑通，零查表失败。所以图块本身是齐的，只是测试图没覆盖全。补测试图即可，不用重画。

---

## 已由脚本自动修复的（无需处理）

见 `ASSET_FIX_REPORT.md`。包括：游离碎屑清除、敌人分层与合并图的 329px 不一致、机柜指示灯、闸门阀轮改色、玄武岩描边增强、色板重出、blob47 掩码表、电缆伤害帧检测。
