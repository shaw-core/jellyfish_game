/**
 * 构建标记。
 *
 * 加这个是因为出过一次事故：一条 shell 命令里 cp 失败导致 `&&` 短路，
 * 补丁根本没执行，而 tsc 与 build 是独立命令照常通过 —— 交付出去的包
 * 看起来是新的，实际代码没变。双方各自以为是对方的问题，来回耗了一轮。
 *
 * 现在标题画面右下角会显示这个版本号。对不上就说明部署的不是这一版，
 * 先查部署，不用争论功能改没改。
 */
export const BUILD_ID = 'b08-zone1-tutorial';
export const BUILD_FEATURES = [
  '主角运动全集（关闭程序变形）',
  '锥形探照灯扫描',
  '开场废墟场 v2',
] as const;
