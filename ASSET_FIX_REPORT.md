# 资产自动修复报告

本文件由 `tools/fix_assets.py` 生成。

- **主角图集**：清除游离碎屑 1073 px（仅处理碎屑 >40px 的帧，thrust 尾部气泡保留）。体积波动超 30% 的 Tag：`charge`, `thrust`, `glide`，已在 JSON 的 `meta.frameQuality` 中标记 `usable:false`。
- **敌人分层**：视锥层压住传感器镜头高光 329 px（合并图里高光在上，分层图里视锥在上）。已从视锥层剔除这些像素并按合并图重建 body 层，修复前差 1711 px，修复后合成结果与合并图完全一致。
- **服务器机柜**：原本两帧仅差 4 px（肉眼不可见），补了 12 个低亮度指示灯点，全灭帧留 3 颗常暗灯 + 1 颗暖色，闪烁帧 3 颗转亮。
- **闸门阀轮**：768 px 由警示红 `#FF3344` 改为锈橙 `#D8792D`，顶部指示灯的红色保留。警示红现在只属于危险源。
- **色板**：重出 `palette.png`（36 色，无抗锯齿，原图因文字抗锯齿含 211 色）。
- **玄武岩描边**：原本露出边缘只有一条极淡亮线，深蓝水体上看不出轮廓。给 52 条露出边补了 2px 描边（外 `#5A4B86` / 内 `#25355F`，均取自色板），内部像素未动，拼接不露缝。
- **自动图块表**：从图块边缘描边反推出 blob47 掩码表，金属与玄武岩各 47 / 47 个唯一掩码，写入 `engine_manifest.json`。
- **电缆伤害帧**：按金色电弧像素自动检测，放电帧为 [19, 20, 21]（0-based），已补进清单 —— 原清单只声明了喷口。
